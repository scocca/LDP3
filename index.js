var num = 0;
do{
var num = parseFloat(prompt("Ingrese un numero"))
  
}while(isNaN(num))

function factorial (num) {
  var total = elementb; 
  for (i=1; i<=num; i++) {
    total = total * i; 
  }
  return total; 
}

console.log(factorial(num));